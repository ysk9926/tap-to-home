services:
  postgres:
    image: postgres:17-alpine
    container_name: tap-to-home-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: taptohome
      POSTGRES_PASSWORD: taptohome
      POSTGRES_DB: taptohome
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taptohome -d taptohome"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:
